/* --- 箭頭函式、ES6 變數、ES6 陣列方法 --- */
let mynumber = [3, 5, 1, 1, 2];
let result = mynumber.filter((item) => item > 2);
console.log(result);
/* --- Class 語法糖 --- */
class Circle { }
/* --- Promise 物件 --- */
const promise = Promise.resolve();

console.log('12456')